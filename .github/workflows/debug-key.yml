name: Debug key prefix

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Show key prefix only
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: 'echo "Key starts with: ${GEMINI_API_KEY:0:6}"'
